# 🌐 Настройка DNS для liksone.ru на Timeweb

## IP сервера: 92.255.108.254
## Домен: liksone.ru

---

## Шаг 1: Войти в панель Timeweb

1. Перейди на [timeweb.com](https://timeweb.com)
2. Войди в личный кабинет
3. В меню слева найди **"Домены"** или **"DNS"**

---

## Шаг 2: Выбрать домен liksone.ru

1. В списке доменов найди **liksone.ru**
2. Нажми на него или нажми кнопку **"Управление"** / **"Настройки"**
3. Найди раздел **"DNS-записи"** или **"Управление DNS"**

---

## Шаг 3: Добавить DNS записи

### Если там уже есть какие-то записи:
- Удали все старые A-записи для `@` и `www`
- Или отредактируй существующие

### Добавь следующие записи:

#### 1️⃣ A-запись для корневого домена

```
Тип:       A
Имя:       @ (или оставь пустым, или liksone.ru)
Значение:  92.255.108.254
TTL:       3600 (или 1 час)
```

#### 2️⃣ A-запись для www

```
Тип:       A
Имя:       www
Значение:  92.255.108.254
TTL:       3600 (или 1 час)
```

### Итоговая таблица DNS записей:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 92.255.108.254 | 3600 |
| A | www | 92.255.108.254 | 3600 |

---

## Шаг 4: Сохранить изменения

1. Нажми **"Сохранить"** или **"Применить изменения"**
2. Подтверди изменения, если потребуется

⏰ **Подожди 5-15 минут** для распространения DNS

---

## Шаг 5: Проверка DNS

### На компьютере:

Открой командную строку (cmd) и выполни:

```cmd
nslookup liksone.ru
nslookup www.liksone.ru
```

Должно показать IP: **92.255.108.254** ✅

### Или через онлайн сервис:

Перейди на:
- https://www.whatsmydns.net/#A/liksone.ru
- https://www.whatsmydns.net/#A/www.liksone.ru

Проверь, что домен резолвится в IP **92.255.108.254** по всему миру.

---

## Шаг 6: Запустить миграцию на сервере

Когда DNS заработает (через 5-15 минут), подключись к серверу:

```bash
ssh root@92.255.108.254
```

И выполни:

```bash
# Создай скрипт
nano migrate-domain.sh

# Вставь содержимое скрипта (из файла migrate-domain.sh)
# Сохрани: Ctrl+O, Enter, Ctrl+X

# Сделай исполняемым
chmod +x migrate-domain.sh

# Запусти
sudo ./migrate-domain.sh
```

Скрипт автоматически:
- ✅ Проверит DNS
- ✅ Создаст конфигурацию Nginx
- ✅ Получит SSL сертификат
- ✅ Настроит HTTPS

---

## 🎉 Готово!

После выполнения всех шагов:
- **liksone.ru** будет доступен по HTTPS
- **Приложение LIKS** будет работать на новом домене
- **vigentop.ru** продолжит работать до удаления

---

## Troubleshooting

### DNS не работает через 15 минут

1. Проверь, что записи добавлены правильно в панели Timeweb
2. Убедись, что используется IP **92.255.108.254**
3. Проверь, что нет конфликтующих записей
4. Попробуй очистить DNS кэш на компьютере:
   ```cmd
   ipconfig /flushdns
   ```

### Ошибка "DNS не настроен" при запуске скрипта

Это значит DNS ещё не распространился. Подожди ещё 5-10 минут и попробуй снова.

### Нужна помощь?

Проверь основную инструкцию в `DOMAIN_MIGRATION.md`
