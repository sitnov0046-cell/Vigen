# 🚀 Деплой на Timeweb с HTTPS

## Шаг 1: Подключение к серверу

```bash
ssh root@your-server-ip
```

## Шаг 2: Загрузка скрипта установки

На сервере выполни:

```bash
# Скачай скрипт установки
wget https://raw.githubusercontent.com/твой-репозиторий/deploy-timeweb.sh
# Или создай вручную
nano deploy-timeweb.sh
```

Скопируй содержимое файла `deploy-timeweb.sh` и вставь в редактор.

```bash
# Сделай скрипт исполняемым
chmod +x deploy-timeweb.sh

# Запусти скрипт
./deploy-timeweb.sh
```

Скрипт автоматически:
- ✅ Установит Node.js 20
- ✅ Установит PM2 для управления процессами
- ✅ Настроит Nginx
- ✅ Установит SSL сертификат от Let's Encrypt
- ✅ Создаст конфигурацию для твоего домена

## Шаг 3: Загрузка кода на сервер

### Вариант А: Через Git (рекомендуется)

1. На локальной машине запушь код в GitHub:
```bash
cd c:\Users\sitno\OneDrive\Рабочий стол\LIKS\webapp
git add .
git commit -m "feat: ready for deployment"
git push origin main
```

2. На сервере клонируй репозиторий:
```bash
cd /var/www/liks
git clone https://github.com/твой-username/твой-репозиторий.git .
```

### Вариант Б: Через FileZilla/SCP

Загрузи все файлы проекта (кроме `node_modules` и `.next`) в `/var/www/liks/`

### Вариант В: Через rsync (быстрый способ)

На локальной машине:
```bash
rsync -avz --exclude 'node_modules' --exclude '.next' --exclude '.git' webapp/ root@your-server-ip:/var/www/liks/
```

## Шаг 4: Установка зависимостей и запуск

На сервере:

```bash
cd /var/www/liks

# Установка зависимостей
npm install

# Генерация Prisma Client
npx prisma generate

# Синхронизация базы данных
npx prisma db push

# Сборка проекта
npm run build

# Запуск с помощью PM2
pm2 start npm --name liks-app -- start

# Сохранение конфигурации PM2
pm2 save

# Автозапуск PM2 при перезагрузке сервера
pm2 startup
# Выполни команду, которую выведет pm2 startup
```

## Шаг 5: Проверка

Открой в браузере: `https://твой-домен.ru`

Если всё настроено правильно, увидишь своё приложение с HTTPS! 🎉

## Управление приложением

### Полезные команды PM2:

```bash
# Статус приложения
pm2 status

# Просмотр логов
pm2 logs liks-app

# Перезапуск после изменений
pm2 restart liks-app

# Остановка
pm2 stop liks-app

# Удаление из PM2
pm2 delete liks-app
```

### Обновление кода:

```bash
cd /var/www/liks

# Через Git
git pull origin main

# Или через rsync с локальной машины
# rsync -avz --exclude 'node_modules' --exclude '.next' webapp/ root@your-server-ip:/var/www/liks/

# Установка новых зависимостей (если есть)
npm install

# Пересборка
npm run build

# Перезапуск
pm2 restart liks-app
```

### Просмотр логов Nginx:

```bash
# Логи доступа
tail -f /var/log/nginx/access.log

# Логи ошибок
tail -f /var/log/nginx/error.log
```

## Настройка DNS

В панели управления Timeweb настрой DNS записи:

1. **A-запись:**
   - Имя: `@`
   - Значение: `IP адрес твоего сервера`
   - TTL: `3600`

2. **A-запись для www:**
   - Имя: `www`
   - Значение: `IP адрес твоего сервера`
   - TTL: `3600`

Подожди 5-15 минут, пока DNS обновятся.

## Подключение к Telegram боту

После успешного деплоя, в коде бота используй:

```python
from telegram import WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup

keyboard = InlineKeyboardMarkup([
    [InlineKeyboardButton(
        text="🎬 Открыть приложение",
        web_app=WebAppInfo(url="https://твой-домен.ru")
    )]
])

await update.message.reply_text(
    "Добро пожаловать в LIKS!",
    reply_markup=keyboard
)
```

## Автоматическое обновление SSL

Certbot автоматически настроил обновление сертификата. Проверить можно:

```bash
# Тестовое обновление
certbot renew --dry-run

# Статус таймера автообновления
systemctl status certbot.timer
```

## Troubleshooting

### Ошибка "Cannot connect to database"
Проверь `DATABASE_URL` в `/var/www/liks/.env`

### Приложение не запускается
```bash
# Проверь логи PM2
pm2 logs liks-app

# Попробуй запустить вручную для отладки
cd /var/www/liks
npm start
```

### Nginx показывает 502 Bad Gateway
```bash
# Проверь, что приложение запущено
pm2 status

# Проверь логи Nginx
tail -f /var/log/nginx/error.log
```

### SSL сертификат не работает
```bash
# Проверь статус сертификата
certbot certificates

# Попробуй получить заново
certbot --nginx -d твой-домен.ru -d www.твой-домен.ru
```

## Мониторинг

### Установка мониторинга PM2 (опционально)

```bash
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 30
```

---

## 🎉 Готово!

Теперь твоё приложение работает на `https://твой-домен.ru` с автоматически обновляемым SSL сертификатом!

**Важные файлы на сервере:**
- Код приложения: `/var/www/liks/`
- Конфиг Nginx: `/etc/nginx/sites-available/liks`
- SSL сертификаты: `/etc/letsencrypt/live/твой-домен.ru/`
- Логи PM2: `~/.pm2/logs/`
