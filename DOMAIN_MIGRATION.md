# 🔄 Миграция домена: vigentop.ru → liksone.ru

## Шаг 1: Настройка DNS для liksone.ru

### В панели управления доменом (например, Timeweb или регистратор домена):

Добавь следующие DNS записи для домена `liksone.ru`:

1. **A-запись для корневого домена:**
   - Тип: `A`
   - Имя: `@` (или пусто)
   - Значение: `IP адрес твоего сервера` (тот же, что у vigentop.ru)
   - TTL: `3600` (1 час)

2. **A-запись для www:**
   - Тип: `A`
   - Имя: `www`
   - Значение: `IP адрес твоего сервера`
   - TTL: `3600`

⏰ **Подожди 5-15 минут**, чтобы DNS записи распространились.

### Проверка DNS:

На сервере выполни:
```bash
# Проверка основного домена
dig liksone.ru +short

# Проверка www
dig www.liksone.ru +short
```

Если видишь IP адрес сервера - DNS настроен правильно! ✅

---

## Шаг 2: Подключение к серверу

```bash
ssh root@твой-server-ip
```

---

## Шаг 3: Обновление конфигурации Nginx

### Создай новый конфиг для liksone.ru:

```bash
nano /etc/nginx/sites-available/liks
```

Вставь следующую конфигурацию:

```nginx
server {
    listen 80;
    server_name liksone.ru www.liksone.ru;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Сохрани файл (`Ctrl+O`, `Enter`, `Ctrl+X`)

### Активируй конфигурацию:

```bash
# Создай символическую ссылку
ln -s /etc/nginx/sites-available/liks /etc/nginx/sites-enabled/

# Проверь конфигурацию
nginx -t

# Если OK, перезапусти Nginx
systemctl reload nginx
```

---

## Шаг 4: Установка SSL сертификата для liksone.ru

```bash
# Установка Certbot (если ещё не установлен)
apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
certbot --nginx -d liksone.ru -d www.liksone.ru --non-interactive --agree-tos --email твой-email@example.com --redirect
```

Certbot автоматически:
- Получит SSL сертификат
- Обновит конфигурацию Nginx
- Настроит редирект с HTTP на HTTPS

---

## Шаг 5: Проверка работы нового домена

Открой в браузере:
- `https://liksone.ru` ✅
- `https://www.liksone.ru` ✅

Должно открыться приложение LIKS с SSL! 🎉

---

## Шаг 6: Обновление Telegram бота

В коде бота замени URL:

```python
from telegram import WebAppInfo, InlineKeyboardButton, InlineKeyboardMarkup

keyboard = InlineKeyboardMarkup([
    [InlineKeyboardButton(
        text="🎬 Открыть приложение",
        web_app=WebAppInfo(url="https://liksone.ru")  # Новый домен!
    )]
])

await update.message.reply_text(
    "Добро пожаловать в LIKS!",
    reply_markup=keyboard
)
```

Перезапусти бота.

---

## Шаг 7: Удаление старого домена (после проверки)

⚠️ **ВАЖНО:** Удаляй старый домен только после того, как убедишься, что новый работает!

### Удали конфигурацию vigentop.ru:

```bash
# Удали символическую ссылку
rm /etc/nginx/sites-enabled/vigentop

# Удали файл конфигурации (опционально)
rm /etc/nginx/sites-available/vigentop

# Проверь конфигурацию
nginx -t

# Перезапусти Nginx
systemctl reload nginx
```

### Отзови SSL сертификат для vigentop.ru (опционально):

```bash
certbot delete --cert-name vigentop.ru
```

---

## Быстрый чеклист ✅

- [ ] DNS записи для liksone.ru настроены
- [ ] DNS записи распространились (dig liksone.ru +short)
- [ ] Nginx конфиг создан и активирован
- [ ] SSL сертификат получен для liksone.ru
- [ ] Приложение открывается на https://liksone.ru
- [ ] Telegram бот обновлён на новый URL
- [ ] Старый домен vigentop.ru удалён из конфигурации

---

## Troubleshooting

### DNS не резолвится
```bash
# Проверь, что DNS записи правильные
dig liksone.ru +short
dig www.liksone.ru +short

# Подожди ещё 10-15 минут для распространения
```

### Nginx ошибка при перезапуске
```bash
# Проверь синтаксис
nginx -t

# Посмотри логи ошибок
tail -f /var/log/nginx/error.log
```

### SSL сертификат не получается
```bash
# Проверь, что домен резолвится на сервер
dig liksone.ru +short

# Проверь, что порт 80 открыт
ufw status

# Попробуй получить сертификат вручную
certbot certonly --nginx -d liksone.ru -d www.liksone.ru
```

### Приложение не работает
```bash
# Проверь, что приложение запущено
pm2 status

# Если нет, запусти
cd /var/www/liks
pm2 start npm --name liks-app -- start

# Проверь логи
pm2 logs liks-app
```

---

## 🎉 Готово!

Теперь твоё приложение работает на новом домене **liksone.ru** и полностью переименовано в **LIKS**!
