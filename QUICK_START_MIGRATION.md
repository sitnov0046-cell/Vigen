# ⚡ Быстрая миграция на liksone.ru

## Шаг 1: Настрой DNS (в панели домена)

В панели управления доменом **liksone.ru** добавь:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | IP твоего сервера | 3600 |
| A | www | IP твоего сервера | 3600 |

⏰ Подожди 5-15 минут

---

## Шаг 2: Запусти скрипт миграции на сервере

```bash
# Подключись к серверу
ssh root@твой-server-ip

# Скачай скрипт (или скопируй вручную)
nano migrate-domain.sh
# Вставь содержимое из migrate-domain.sh

# Сделай исполняемым
chmod +x migrate-domain.sh

# Запусти
sudo ./migrate-domain.sh
```

Скрипт попросит:
- Email для SSL - введи свой email
- Подтверждение - напиши `y`

Скрипт автоматически:
- ✅ Проверит DNS
- ✅ Создаст конфигурацию Nginx
- ✅ Получит SSL сертификат
- ✅ Настроит HTTPS

---

## Шаг 3: Проверь новый домен

Открой в браузере: **https://liksone.ru**

Должно открыться приложение LIKS с SSL! ✅

---

## Шаг 4: Обнови Telegram бота

В коде бота замени URL:

```python
web_app=WebAppInfo(url="https://liksone.ru")
```

Перезапусти бота и протестируй.

---

## Шаг 5: Удали старый домен (после проверки)

На сервере:

```bash
# Удали конфиг старого домена
rm /etc/nginx/sites-enabled/vigentop

# Проверь и перезапусти Nginx
nginx -t && systemctl reload nginx

# Удали SSL сертификат (опционально)
certbot delete --cert-name vigentop.ru
```

---

## 🎉 Готово!

Приложение переехало на **liksone.ru**!

---

## Если что-то не работает

### DNS не резолвится
```bash
dig liksone.ru +short
# Если пусто - подожди ещё или проверь настройки DNS
```

### SSL ошибка
```bash
# Проверь, что DNS указывает на сервер
dig liksone.ru +short

# Попробуй получить сертификат заново
certbot --nginx -d liksone.ru -d www.liksone.ru
```

### Приложение не запущено
```bash
pm2 status
pm2 logs liks-app

# Если не запущено
cd /var/www/liks
pm2 start npm --name liks-app -- start
```

---

## Помощь

Если нужна помощь - проверь подробную инструкцию в `DOMAIN_MIGRATION.md`
